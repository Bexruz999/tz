🚀 Технологический стек

Язык: PHP 8.4.

Архитектура: Единая точка входа (public/index.php) с маршрутизацией на базе Symfony Routing.

База данных: MySQL 8.0.

Шаблонизатор: Smarty 5.

Контейнеризация: Docker (Nginx, PHP-FPM 8.4, MySQL 8.0).

🛠 Установка и запуск

1. Клонирование репозитория

git clone https://github.com/Bexruz999/tz.git
cd tz


2. Настройка окружения

Создайте файл .env в корне проекта на основе примера:

DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=tz_database
DB_USERNAME=root
DB_PASSWORD=root_password


3. Запуск через Docker

docker-compose up -d


4. Установка зависимостей

docker-compose exec app composer install


5. Миграции и наполнение данными (Seeding)

Чтобы протестировать систему на большом объеме данных:

# Создание таблиц
docker-compose exec app php db/migrate.php
# Генерация тестовых данных
docker-compose exec app php db/Seeder.php
